---
slug: booking-flow
job: 1
date: 2026-08-20
model: claude-fable-5
effort: unknown
---

# Touchdown — booking-flow #1

## Aim
Give the DAS contact page the ODNDR booking architecture: a follow-up selector (book a strategy call / custom pricing / written reply), form-first submission, and the live calendar on success for the book path.

## What it was told
Jayden: replicate the offdutynotdrivingrewards.com contact flow on the driver appreciation site — pricing requests stay, but prospects can also pick options including booking a call. He is creating the DAS iClosed event himself; the site must work before and after that link exists.

## What it did
- files changed: `contact.html` (follow-up selector cards + `#cr-book-success` calendar container + dynamic submit label via `#cr-submit-label` + `followUp` in payload + success branching with iClosed iframe prefill), `api/contact.js` (default handler gains `followUp` → "Requested Follow-up" row + subject tag "· WANTS TO BOOK A CALL" / "· WANTS CUSTOM PRICING")
- commit: "contact: follow-up selector + booking path — the ODNDR flow, DAS-styled" on master (das-website repo)
- tree: clean after commit
- tests/build: `node --check` green on api/contact.js and the extracted inline script; live verification post-deploy (CSP check done up front: DAS sends no CSP header, so the iframe embed cannot repeat the ODNDR frame-src block)
- deploy: `vercel --prod`, project `website` (confirmed via .vercel/project.json — NOT das-portal-ten, per the standing deploy-target rule)

## How it went
Grade: 4 — clean graft onto the incumbent page: reused the existing cf-* form grammar, success-state machinery, 10DLC consent block, and the speed-to-lead forward untouched; the selector and calendar are additions, not rewrites. The DAS event doesn't exist yet, so `DAS_ICLOSED_URL` ships empty and the book path degrades honestly ("we'll email you times to pick from") — no dead iframe, no fake calendar. One paste + redeploy activates it. The CSP lesson from ODNDR (job 19 in that repo: element-presence is not a load check; check the EMBEDDING side's headers) was applied before writing a line: DAS's vercel.json sends no Content-Security-Policy, only X-Frame-Options (the opposite direction), so the embed is clear.

Two button-label bugs caught during the build, not after: `btn.textContent = 'Sending…'` (and the error-path restore) would have destroyed the label span and the arrow svg — both now write through `#cr-submit-label`.

## Correction passes
0

## Any errors
- none at commit time; live verification runs post-deploy. status: open
