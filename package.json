{
  "name": "driver-appreciation-solutions",
  "version": "1.0.0",
  "description": "Driver Appreciation Solutions — fleet recognition e-commerce site",
  "private": true,
  "scripts": {
    "dev": "echo 'Static site — run: npx vercel dev --listen 8888'",
    "build": "echo 'Static site — no build step required'",
    "deploy": "npx vercel --prod"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.0",
    "@sparticuz/chromium": "^131.0.0",
    "@supabase/supabase-js": "^2.45.0",
    "puppeteer-core": "^23.11.1",
    "stripe": "^14.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
